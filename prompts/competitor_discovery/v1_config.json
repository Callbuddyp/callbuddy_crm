{
  "json_schema": {
    "type": "array",
    "items": {
      "type": "object",
      "required": ["name", "aliases", "rationale", "discovered_urls", "focus_areas"],
      "additionalProperties": false,
      "properties": {
        "name": {
          "type": "string",
          "description": "Official company name as used in Denmark"
        },
        "aliases": {
          "type": "array",
          "items": { "type": "string" },
          "description": "Alternative names, brands, or sub-brands customers use"
        },
        "rationale": {
          "type": "string",
          "description": "One sentence explaining why this company is a relevant competitor"
        },
        "discovered_urls": {
          "type": "array",
          "items": { "type": "string" },
          "description": "Real URLs found during search: pricing pages, Trustpilot, news articles"
        },
        "focus_areas": {
          "type": "string",
          "description": "Campaign-specific comparison angles based on actual findings"
        }
      }
    }
  }
}